[build]
  command = "npm run build"
  publish = "build"  # For React apps (create-react-app)

[[redirects]]
  from = "/api/*"
  to = "https://notesapp-a6cuddbjdkeza7ft.canadacentral-01.azurewebsites.net/api/:splat"
  status = 200
  force = true

# Optional: Add CORS headers
[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
